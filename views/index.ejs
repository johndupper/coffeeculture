<!DOCTYPE html>
<html lang="en">
<head>
    <% include partials/head %>
</head>

<body class="container-fluid">
    <header>
        <% include partials/header %>
    </header>

    <% if (!currentUser) { %>
        <div class="container">
            <div class="jumbotron col-sm-8 col-sm-offset-2">
                <h2>Welcome</h2>
                <h4>Please sign up or log in</h4>
                <a class="btn btn-info btn-lg" href="signup" role="button">Sign Up</a>
                <a class="btn btn-warning btn-lg" href="login" role="button">Log In</a>
            </div>
        </div> <!-- body wrapper -->
    <% } else { %>
        <div class="container">
            <h4>Coffee!!</h4>
        </div>
    <% } %>

    <footer>
        <% include partials/footer %>
    </footer>
</body>
</html>
