<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
</head>

<body class="container-fluid">
    <header>
        <% include ../partials/header %>
    </header>

    <main>
        <h3>Edit</h3>

        <form class="todo-form" action="/posts/<%= post._id %>?_method=PUT" method="post">
            <% include _form %>
            <a href="/<%= post._id %>" class="btn btn-signup">Back</a>
            <button type="submit" class="btn btn-login">Save</button>
        </form>
    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>
</body>
</html>